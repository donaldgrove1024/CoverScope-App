{
  "name": "coverscope-app",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server.js",
    "emulators": "npx -y firebase-tools@latest emulators:start --only auth,firestore --project coverscope-ac374",
    "test": "node --experimental-vm-modules ./node_modules/jest/bin/jest.js --runInBand --forceExit",
    "test:emulators": "npx -y firebase-tools@latest emulators:exec --only auth,firestore --project coverscope-ac374 npm test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^12.18.0"
  },
  "devDependencies": {
    "jest": "^30.5.1",
    "vite": "^8.2.2"
  }
}
